# first-pr-practice

A tiny practice repo for learning the GitHub PR workflow.

## What's here

`greet.py` is a small script that prints a friendly greeting.

```bash
python3 greet.py Teru
# Hello, Teru!
```

Run it with no arguments and it greets "world" by default.
